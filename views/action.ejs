<!DOCTYPE html>
<html>
<head>
  <title>Workflow Runs</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen py-10 px-6">
  <div class="absolute top-6 right-6">
    <a href="/" class="bg-red-500 hover:bg-red-600 text-white py-2 px-4 rounded-xl">Logout</a>
  </div>

  <h1 class="text-3xl font-bold mb-6 text-center">Workflow Runs</h1>
  <% if (data.length !== 0) { %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% data.forEach((e) => { %>
        <div class="bg-gray-800 p-6 rounded-2xl shadow-md hover:shadow-xl transition">
          <p><strong>Name:</strong> <%= e.name %></p>
          <p><strong>ID:</strong> <%= e.id %></p>
          <p><strong>Status:</strong> <%= e.status %></p>
          <p><strong>Conclusion:</strong> <%= e.conclusion || 'N/A' %></p>
          <p><strong>Created At:</strong> <%= e.created_at %></p>
        </div>
      <% }) %>
    </div>
  <% } else { %>
    <p class="text-center text-gray-400">No CI/CD Pipelines found.</p>
  <% } %>
</body>
</html>
