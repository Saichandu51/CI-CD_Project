<!DOCTYPE html>
<html>
<head>
  <title>GitHub Auth App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white flex items-center justify-center h-screen">
  <div class="absolute top-6 right-6">
    <a href="/" class="bg-red-500 hover:bg-red-600 text-white py-2 px-4 rounded-xl">Logout</a>
  </div>

  <div class="text-center">
    <h1 class="text-4xl font-bold mb-8">Welcome to GitHub Auth</h1>
    <% if (user) { %>
      <p class="mb-4 text-xl">Hello, <%= user.username %>!</p>
      <a href="/profile" class="bg-green-600 hover:bg-green-700 text-white py-2 px-6 rounded-xl transition-all">Go to Profile</a>
    <% } else { %>
      <a href="/auth/github" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-6 rounded-xl transition-all">Login with GitHub</a>
    <% } %>
  </div>
</body>
</html>
